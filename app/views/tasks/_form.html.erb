<!-- タスクフォーム パーシャル -->
<!-- タスク作成・編集 -->

<%= form_with model: [@user, @task], local: true do |f| %>
    
  <%= render 'shared/error_messages', object: f.object %>
  
  <%= f.label :content, class: "label-form" %>
  <%= f.text_field :content, class: "form-control" %>
  <%= f.label :description, class: "label-form" %>
  <%= f.text_area :description, rows: 5, class: "form-control" %>
  <%= f.submit "#{yield(:btn_text)}", class: "btn #{yield(:btn_size)} btn-primary btn-form" %>
  
  <% if @path.is('tasks#update') || @path.is('tasks#edit') %>
    <%= link_to "キャンセル", user_tasks_path, class: "btn btn-default btn-form" %>
  <% end %>

<% end %>

